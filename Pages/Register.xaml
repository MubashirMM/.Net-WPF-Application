<Page x:Class="WpfApp1.Pages.Register"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Register" Height="600" Width="500">

    <Grid Background="#F4F4F4">
        <Border Background="White" CornerRadius="10" Padding="20" 
                VerticalAlignment="Center" HorizontalAlignment="Center" Width="350">
            <StackPanel>
                <TextBlock Text="Create Account" FontSize="24" FontWeight="Bold" 
                           HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <Label Content="Full Name" FontWeight="SemiBold"/>
                <TextBox Name="NameTextBox" Height="32" VerticalContentAlignment="Center"/>
                <TextBlock Name="ErrorName" Visibility="Collapsed" Foreground="Red" Text="* Name is required" FontSize="11"/>

                <Label Content="Email" FontWeight="SemiBold" Margin="0,10,0,0"/>
                <TextBox Name="EmailTextBox" Height="32" VerticalContentAlignment="Center"/>
                <TextBlock Name="ErrorEmail" Visibility="Collapsed" Foreground="Red" Text="* Enter a valid email" FontSize="11"/>

                <Label Content="Password" FontWeight="SemiBold" Margin="0,10,0,0"/>
                <PasswordBox Name="PasswordTextBox" Height="32" VerticalContentAlignment="Center"/>
                <TextBlock Name="ErrorPassword" Visibility="Collapsed" Foreground="Red" Text="* Password is required" FontSize="11"/>

                <Label Content="Select Role" FontWeight="SemiBold" Margin="0,10,0,0"/>
                <ComboBox Name="RoleComboBox" SelectedIndex="0" Height="32">
                    <ComboBoxItem Content="-- Select Role --" Tag="0" IsEnabled="False"/>
                    <ComboBoxItem Content="SuperAdmin" Tag="1"/>
                    <ComboBoxItem Content="Admin" Tag="2"/>
                    <ComboBoxItem Content="User" Tag="3"/>
                </ComboBox>
                <TextBlock Name="ErrorRole" Visibility="Collapsed" Foreground="Red" Text="* Please select a role" FontSize="11"/>

                <Button Content="Register" Margin="0,30,0,0" Height="40" 
                        Background="#2196F3" Foreground="White" FontWeight="Bold"
                        Click="BtnRegister_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>